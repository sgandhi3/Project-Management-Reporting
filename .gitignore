node_modules/
.env
# Per-machine Claude Code settings (accumulates tool-permission approvals
# every run — should never be tracked, was committed by mistake)
.claude/settings.local.json
# Generated outputs — keep temp2.pptx (template) but ignore reports
Report_*.pptx
report.pptx
new temp.pptx
ai-narrative-input.json
.gather-cache.json
*.xlsx
~$*
.DS_Store
